# DocuGenius Windows 依赖包列表
# 核心文档处理包
python-docx>=0.8.11
python-pptx>=0.6.21
openpyxl>=3.0.10

# PDF处理包（轻量级方案）
pdfplumber>=0.7.0      # 轻量级PDF文本提取（0.8MB，不支持图像提取）

# 可选增强包
# Pillow>=9.0.0        # 图像处理增强（如需要）
# requests>=2.28.0     # 网络功能（如需要）
